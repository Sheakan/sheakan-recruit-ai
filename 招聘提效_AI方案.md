# 基于 AI 的招聘数据自动化落地方案（产品名：智聘 RecruitMind）

> 群面作业 · 招聘提效场景
> 目标：在最小成本预算下，用 AI 替代 HR 手工记录招聘数据，实现自动化记录、实时同步、可视化看板，最终解放人工。

---

## 一、背景与痛点

现有招聘流程中，HR 的日常工作载体为 **企业微信、企业微信群、腾讯在线文档**。核心痛点：

| 痛点 | 具体表现 | 业务影响 |
|------|----------|----------|
| 人工录入效率低 | 候选人状态变更需手动复制粘贴到文档 | 重复劳动，占用 30%~50% 事务性工时 |
| 数据易遗漏 | 群消息、语音、零散沟通中信息分散 | 关键节点（面试/offer）漏跟，体验差 |
| 时效性差 | 文档更新滞后于实际进展 | 管理层看板数据失真，决策延迟 |

**根因**：招聘信息本质是"非结构化自然语言"（群聊、语音、随手记），而管理需要"结构化数据"（候选人 / 岗位 / 阶段 / 状态 / 时间）。中间的转化全靠人肉完成。

---

## 二、方案目标

1. **自动化记录**：自然语言 / 群消息 → 自动转为结构化招聘数据，零手工录入。
2. **实时同步更新**：数据源唯一，录入即同步，看板即时刷新。
3. **数据可视化**：招聘漏斗、各阶段人数、转化率、平均周期一目了然。
4. **最小成本**：优先使用免费模型与免费组件，月度成本趋近于 0。

---

## 三、整体架构

```
┌─────────────────────────────────────────────────────────────┐
│  ① 输入 / 数据源层                                           │
│   企业微信群机器人  │  HR 网页/IM 录入  │  语音/附件转写        │
└───────────────────────────┬─────────────────────────────────┘
                            │  Webhook / API
┌───────────────────────────▼─────────────────────────────────┐
│  ② AI 解析层（大模型 + 提示词工程）                           │
│   非结构化文本 → 结构化字段（候选人/岗位/阶段/状态/时间/面试官）│
│   低置信度 → 自动标记待人工确认                                │
└───────────────────────────┬─────────────────────────────────┘
                            │  结构化数据写入
┌───────────────────────────▼─────────────────────────────────┐
│  ③ 数据层（单一可信源）                                       │
│   腾讯文档智能表  /  轻量数据库（localStorage / CloudBase）    │
└───────────────────────────┬─────────────────────────────────┘
                            │  实时读取
┌───────────────────────────▼─────────────────────────────────┐
│  ④ 展示 / 应用层                                             │
│   实时看板（漏斗/转化/时效）│ 自动提醒  │ 周报自动生成          │
└─────────────────────────────────────────────────────────────┘
                            ↑
                    反馈闭环：异常/待确认回流 HR
```

> 架构图见同目录 `招聘提效_架构图.svg`

---

## 四、AI 应用设计（核心能力）

### 4.1 大模型结构化抽取

把 HR 的随手记录（示例）：
> "张三 投递了后端实习岗，已通过初筛，安排周四下午面试，面试官李四"

通过提示词工程抽取为统一结构：

```json
{
  "candidate": "张三",
  "position": "后端实习",
  "stage": "面试",
  "status": "已安排",
  "interviewer": "李四",
  "scheduled_time": "2026-08-28 14:00",
  "source": "企业微信群"
}
```

**标准字段 Schema**（可扩展）：
`候选人 / 岗位 / 招聘阶段(投递→初筛→面试→offer→入职) / 状态 / 面试官 / 时间 / 渠道来源 / 置信度`

### 4.2 提示词要点

- 角色：招聘数据录入助手
- 任务：仅输出 JSON，字段缺失填空字符串，不自造
- 校验：给出 `confidence` 字段；低于阈值标记 `need_review=true`
- 兜底：无法解析时返回统一错误结构，前端提示人工补录

### 4.3 多模态扩展（加分项）

- 语音消息 → 调用免费 ASR（或模型多模态）→ 文本 → 同上解析
- 简历附件 → 解析关键字段自动建卡

---

## 五、技术选型与成本（最小预算）

| 组件 | 方案 | 费用 |
|------|------|------|
| AI 模型 | 本地 `Ollama+Qwen2.5-7B` **或** 云端免费额度（Doubao / Qwen / 智谱 GLM） | **¥0 ~ 几元/月** |
| 数据存储 | 腾讯文档智能表（HR 已用，零学习成本） | 免费 |
| 消息接入 | 企业微信群机器人 Webhook | 免费 |
| 可视化 | ECharts / Chart.js（开源） | 免费 |
| 部署 | CloudBase / Vercel 免费额度 | 免费 |
| **合计** | | **≈ ¥0 / 月** |

> 成本核心只在"模型调用"，而本场景数据量极小（每日数十条），免费额度即可覆盖，真正零成本可落地。

---

## 六、落地路径（分阶段）

- **Phase 1（Demo 验证，本次作业）**：纯前端 HTML，粘贴/模拟群消息 → 调免费模型 → 结构化 → ECharts 看板。浏览器打开即用，证明 AI 解析 + 可视化能力。
- **Phase 2（群消息接入）**：企业微信群机器人接收消息 → 云函数调用模型解析 → 写入腾讯文档智能表。
- **Phase 3（闭环智能化）**：看板实时联动 + 节点超时自动提醒 + 周报自动生成，完全替代手工。

---

## 七、可运行 Demo 说明（本次交付）

**形态**：单个 HTML 文件，无需后端即可运行（模型调用走免费云 API 或本地 Ollama）。

**功能**：
1. 输入框：模拟"企业微信群消息"或 HR 随手记。
2. 点击解析：调用大模型，返回结构化招聘数据。
3. 自动入库：追加到数据表（localStorage 演示）。
4. 实时看板：招聘漏斗、各阶段人数、转化率、平均周期随录入即时刷新。

**交付物**：
- 代码链接（仓库 / 文件）
- 在线预览（部署到免费静态托管）
- 架构图 + 本方案文档

> 体现"AI 工具/模型的实际应用能力"：真实调用大模型完成非结构化→结构化转化，而非规则匹配或写死模板。

---

## 八、预期收益

- HR 事务性录入工时下降 **60%~80%**
- 数据遗漏率趋近 **0**，看板实时可信
- 招聘周期可量化，管理层决策提速
- 全程近零成本，ROI 极高

---

## 九、风险与对策

| 风险 | 对策 |
|------|------|
| 模型解析偶发错误 | `confidence` 阈值 + 待确认标记，关键节点人工兜底 |
| 企业微信/文档权限 | Phase 1 用前端 Demo 规避，真实接入走官方开放能力 |
| 数据隐私 | 优先本地模型（Ollama）或私有化部署，敏感字段脱敏 |
| 免费额度限制 | 多模型可切换接口，量小几乎无影响 |

---

*本方案聚焦"用 AI 把人从重复录入中解放出来"，以最小成本实现可运行、可演示、可落地的招聘提效。*

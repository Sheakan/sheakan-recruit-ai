# 智聘 · 招聘数据智能中枢（RecruitMind）作业说明

> 群面作业主题：**用低成本 AI 替代 HR 手工录入，提升招聘数据处理效率**
> 提交形式建议：① 本说明文档 + ② 系统在线演示（CloudBase 部署链接）+ ③ 现场 3 分钟讲解（重点讲"问题—方案—限制—展望"）。

---

## 一、项目背景与痛点

招聘过程中，HR 每天要在企业微信群、邮件、简历附件、聊天记录之间手动搬运大量信息：

- 候选人进展散落在群消息、语音转写、PDF 简历、截图中，**没有统一结构**；
- 录入到在线表格（企业微信智能表 / 腾讯文档）**全靠手工复制粘贴**，重复且易错；
- 招聘漏斗、渠道转化等数据**无法实时可视化**，决策靠拍脑袋。

本项目用"大模型结构化抽取 + 自动同步 + 可视化看板"把这条链路自动化。

## 二、功能亮点

| 能力 | 说明 |
|---|---|
| 多模态录入 | 支持粘贴招聘文本、上传**简历 PDF**、上传**招聘截图/简历照片**（视觉模型识别图中文字再结构化） |
| 智能结构化 | 智谱 GLM 自动抽取 21 个字段：候选人、性别、年龄、岗位、部门、阶段、状态、面试官、招聘负责人、渠道、学历、工作年限、当前公司、期望薪资、联系方式、时间、备注、手机号、邮箱、期望城市、来源 |
| 实时看板 | 招聘漏斗、各岗位分布、渠道分布、状态分布四类图表 + 关键指标（总记录/去重候选人/已入职/异常预警） |
| 一键同步 | 录入即自动写入**企业微信智能表**与**腾讯文档在线表格**，替代手工录入 |
| 自服务配置 | 模型类型、智能表 WEBHOOK、字段映射、腾讯文档凭证均可在界面配置，无需改代码 |
| 可修正 | 每条记录支持「编辑 / 删除」，允许人工校正 AI 抽取误差 |
| 大模型理解与生成 | 内置「AI 助手」：用中文**问答招聘数据**（如"二面多少人"）、一键**生成招聘周报**（进展/风险/建议）；抽取结果带**置信度**，低分标黄建议复核 |
| 异常预警 | 看板自动检测"缺联系方式 / 阶段停滞超 7 天 / 薪资异常"等，红字提示待跟进项 |

## 三、技术架构

```
采集层（多入口，均免域名认证）：
  · Web 录入页（手机/电脑）→ /api/extract「函数提取消息」HTTP 接口
  · 企业微信智能表（HR 原生收集）→ 云函数定时 / 手动「轮询拉取」→ AI 提取
  · 简历 PDF(pdfplumber) / 招聘截图(视觉模型)
   ↓
解析层（云函数 SCF 为数据处理中枢）：智谱 GLM（文本 glm-4-flash / 视觉 glm-4v-plus），
          Prompt 约束输出 21 字段 JSON；模型类型界面可配（可换 DeepSeek / 豆包等）
   ↓
存储层：服务端 records.json（轻量演示存储，去重写入）
   ↓
同步层：企业微信智能表 Webhook（追加）+ 腾讯文档 MCP（`sheet.set_range_value` 全量覆盖）
   ↓
展示层：ECharts 看板 + 明细表（前端 index.html）
```

- 后端：Flask（Python），单文件服务 `server.py` + 可插拔模块（`ai_parse` / `smartsheet_store` / `tencent_docs_store` / `config_store` / `wecom_smartsheet`）
- 部署：以**腾讯云函数 SCF（Web 函数）**为默认形态运行（见 `scf_bootstrap` / `app.py` / `cloudbaserc.json`），也可跑在 CloudBase 云托管 / Render 等任意支持 Python 的云平台，凭证经环境变量或界面注入

## 四、使用步骤（演示脚本）

1. 打开系统 → 点「配置我的凭证」填入**智谱 GLM API Key**（必填）。
2. （可选）填入企业微信智能表 WEBHOOK、腾讯文档应用凭证、选择模型。
3. 在左侧录入区：
   - 粘贴一段招聘进展文本，点「手动解析录入」；或
   - 上传一份简历 PDF / 一张招聘截图。
4. 右侧看板实时更新；点「同步到企业微信智能表 / 腾讯文档」把数据写入原文档。
5. 点「载入演示数据」可一键填充 12 条样例，快速展示看板效果。

## 五、企业微信消息获取的限制与解法

企业微信「接收消息」回调要求回调 URL 的域名**已完成 ICP 备案且主体为企业自身**，这是平台硬限制，云函数或任何中转都无法绕过。

为此本系统提供一条**不依赖回调域名**的输入通道：HR 在企业微信**智能表**里直接收集或填写招聘数据，部署在云函数上的本系统**定时主动拉取**新增行 → AI 提取 → 写入腾讯文档与看板（使用应用 corpid/corpsecret 读取智能表，无需回调域名、无需 Token/AESKey）。点「从企业微信智能表拉取」即可手动触发，也可由 SCF 定时触发器自动执行。

群消息实时接收（回调模式）仍需要企业自有备案域名或开通**会话内容存档**（官方付费 + 员工授权）才能启用；接口 `/wecom` 已完整保留，配置后即可无缝切换，解析与同步逻辑无需改动。个人微信没有官方群消息接口，第三方"个人微信机器人"属逆向协议，易被封号且违反服务条款，不建议生产使用。

当前方案以「Web 录入 / 上传文件 / 智能表轮询拉取」完整演示数据流，输出端（企业微信智能表、腾讯文档）真实可用；具备备案域名或开通存档后，可进一步升级为群消息实时接收。

## 六、未来展望

- **实时接入**：绑定企业自有备案域名或开通会话存档后，自动实时拉取群消息并录入。
- **聊天记录导入桥**：支持导入微信聊天记录导出文件（CSV/Excel），自动映射到 21 字段，打通开源提取工具。
- **多模型与置信度（已实现）**：已支持界面切换模型（智谱/DeepSeek/豆包等），并按抽取**置信度**自动标记待人工复核项；自然语言问答与 AI 周报已上线。
- **候选人体验**：去重合并、阶段自动推进提醒、看板导出 BI 报表，支撑招聘漏斗精细化运营。
- **权限与协作**：多 HR 协同、操作审计、字段级权限，向团队级招聘中台演进。

## 七、部署与提交建议

- **部署（推荐：腾讯云函数 SCF）**：在 SCF 控制台新建「Web 函数」→ 运行环境 Python3.11 → 上传本仓库 → 启动命令填 `scf_bootstrap`（事件函数模式则用 `app.py` 的 `handler`，已内置 `serverless-wsgi` 适配）。配置 `ZHIPU_API_KEY` 等环境变量（或界面配置）即可。项目已附 `cloudbaserc.json`（含 SCF 函数与 API 网关触发器配置）。
- **本地 / 其他云**：`pip install -r requirements.txt` → 配置 `ZHIPU_API_KEY` 等环境变量（或界面配置）→ `python server.py`；CloudBase 云托管、Render 等同理。
- **"函数提取消息"落地**：SCF 部署后，Web 录入页即为「函数提取消息」入口；若启用智能表轮询，在 SCF 建一个**定时触发器**（如每 5 分钟）调用本系统的 `/api/poll_smartsheet`，即可自动从企业微信智能表拉取并提取，全程无需域名认证。
- **讲解建议**：先抛痛点（手工录入低效易错）→ 现场演示"上传简历/截图→自动结构化→看板+同步"或"智能表轮询拉取"→ 讲清"云函数不能绕过域名认证，因此用『函数轮询提取』解决"的工程权衡。
- **已知不足**：当前为演示级存储（单文件 JSON，非数据库）；演示数据为原料合成；群消息实时接收受备案/存档限制（已用免域名的智能表轮询作为可用替代）。这些都已在"未来展望"给出演进路径。
